Just compile a damn blog

Take a directory of posts, a directory of assets, and some config settings, and compile that into a directory of interlinking html pages. Each page relies on no other assets; the (ideally minimal) CSS is included on each page.